{
  "name": "hkatalk-monorepo",
  "version": "1.0.0",
  "description": "Deployment configuration for Railway.app",
  "scripts": {
    "postinstall": "cd server && npm install && cd ../client && npm install",
    "build": "cd client && npm run build",
    "start": "cd server && npm start"
  },
  "engines": {
    "node": "20.x"
  }
}
